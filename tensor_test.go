package tensor

import (
	"testing"
)

func TestBasic(t *testing.T) {
	// aF64 := New[float64](WithShape(2, 3), WithBacking([]float64{1, 2, 3, 4, 5, 6}))
	// bF64 := S(2.9)
	// var a NumValue[float64] = aF64
	// var b NumValue[float64] = bF64

	// cF64, err := Add(a, b)
	// t.Logf("%v | %v", cF64, err)
	// t.Logf("%v", cF64.Data())
	// _ = cF64
	// _ = err

	// var c Value[float64] = cF64
	// cprime, err := Transpose(c)
	// t.Logf("cPrime %v | err %v", cprime, err)
	// t.Logf("%v", cprime.Data())

	// var d = cprime.(NumValue[float64])
	// t.Logf("%v", d)
}
